import 'package:flutter/material.dart';

class FeaturesItemWidget extends StatelessWidget {
  const FeaturesItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 330,
      child: Container(
        
      ),
    );
  }
}